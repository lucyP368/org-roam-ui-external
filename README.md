This is a fork of [org-roam-ui](https://github.com/org-roam/org-roam-ui), which allows users to host a web server that interacts with Org-roam in Emacs for side-by-side use. This fork very simply extends the functionality to enable the UI to be served externally.